// app/lib/constants.js
// 애플리케이션 전역 상수 정의

/**
 * ==============================================================================
 * API 관련 상수
 * ==============================================================================
 */

// FastAPI 베이스 URL
export const FASTAPI_BASE_URL = "http://202.20.84.65:8083/api/v1";

// 기본 API 파라미터
export const API_DEFAULTS = {
  TENANT_ID: 1000,
  STAGE_ID: "DEV",
  SEC_OFC_ID: "000025",
};

// 메시지 페이지네이션
export const MESSAGE_LIMIT = 15;

/**
 * ==============================================================================
 * 타임아웃 및 인터벌 상수
 * ==============================================================================
 */

// HTTP 요청 타임아웃 (밀리초)
export const TIMEOUTS = {
  DEFAULT_REQUEST: 5000,      // 5초 - 일반 API 요청
  LLM_REQUEST: 30000,          // 30초 - LLM API 요청
};

// 폴링 인터벌 (밀리초)
export const POLLING_INTERVALS = {
  DEV_MEMOS: 5000,             // 5초 - 개발 메모 폴링
};

// 캐시 기간 (밀리초)
export const CACHE_DURATIONS = {
  SCENARIO_CATEGORIES: 5 * 60 * 1000,  // 5분
  QUERY_STALE_TIME: 60 * 1000,          // 1분
};

// 토스트 표시 시간 (밀리초)
export const TOAST_DURATION = 3000;  // 3초

// 스플래시 화면 시간 (밀리초)
export const SPLASH_DURATION = 1000;  // 1초

/**
 * ==============================================================================
 * URL 관련 상수
 * ==============================================================================
 */

// 자동 팝업 대상 URL
export const TARGET_AUTO_OPEN_URL = 
  "http://172.20.130.91:9110/oceans/BPM_P1002.do?tenId=2000&stgId=TST&pgmNr=BKD_M3201";

// 자동 팝업 완료 메시지
export const AUTO_OPEN_COMPLETE_MESSAGE = "e-SOP 링크 호출 완료했습니다.";

// 기본 부모 Origin
export const DEFAULT_PARENT_ORIGIN = "http://localhost:3000";

/**
 * ==============================================================================
 * UI 관련 상수
 * ==============================================================================
 */

// 시나리오 패널 너비
export const SCENARIO_PANEL_WIDTH = 784;

// 부모 애니메이션 딜레이
export const PARENT_ANIMATION_DELAY_MS = 300;

/**
 * ==============================================================================
 * 임시 ID 접두사
 * ==============================================================================
 */

export const TEMP_ID_PREFIX = "temp_";

/**
 * ==============================================================================
 * 정규식 패턴 상수
 * ==============================================================================
 */

// 정규식 특수문자 이스케이프 함수
export const escapeRegExp = (string) => {
  return string.replace(/[.*+?^${}()|[\]\\]/g, '\\$&'); 
};
